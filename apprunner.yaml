version: 1.0
runtime: python3.8
build:
  build_command: pip install -r requirements.txt
  environment_variables:
    - name: FLASK_ENV
      value: production
deployment:
  command: "python app.py"
  ports:
    - 5000
  environment_variables:
    - name: FLASK_APP
      value: app.py
