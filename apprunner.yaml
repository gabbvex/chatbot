version: 1.0
runtime: python3
build:
  commands:
    - pip install -r requirements.txt
environment:
  variables:
    FLASK_ENV: production
source:
  image:
    repository: https://github.com/gabbvex/chatbot
    tag: latest
    port: 5000
